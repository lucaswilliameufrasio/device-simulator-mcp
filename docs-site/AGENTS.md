## Development

- Use Node.js 24.21.0 or newer.
- Use pnpm 12.4.1 through the `packageManager` field.
- Use Tailwind CSS utilities for page styling; keep custom CSS limited to
  project-specific visual treatments.
- Run `pnpm build` after page changes.
